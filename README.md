# Tetris Core Logic

This repository contains small, pure utility functions for Tetris scoring and level progression.

## Commands

- `npm run build` - Syntax-checks the source files.
- `npm test` - Runs unit tests with Node's built-in test runner.
